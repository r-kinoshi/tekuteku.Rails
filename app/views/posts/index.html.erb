<div class="posts">
  <div class="posts__image">
    <img src="TAKE OUT2.png">
  </div>
  <div class="posts__list">
    <% @posts.each do |post| %>
      <%= link_to post.title, post_path(post),  method: :delete, data: { confirm: '本当に削除してもよろしいですか？' } %>
    <% end %>
  </div>
</div>